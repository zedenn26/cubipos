import { Workspace } from "@/components/workspace";
import { Returns } from "@/components/operations/returns";
export default function Page() {
  return (
    <Workspace title="Returns & exchanges">
      <Returns />
    </Workspace>
  );
}
